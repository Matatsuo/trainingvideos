import React from 'react';

const NotFoundPage = () => (
  <div className="page-container">
    <h1 className="page-header landing">Page does not exist</h1>
    <div className="landing-container">
      <div className="landing-description">
        <p>Try clicking on one of the pages above to get back on track.</p>
      </div>
    </div>
  </div>
);

export default NotFoundPage;
