import React from 'react';

const NotFoundPage = () => (
  <div className="pageContainer">
    <h1 className="pageHeader landing">This page does not exist</h1>
    <div className="landingContainer">
      <div className="landingDescription">
        <p>Try clicking on one of the pages above to get back on track.</p>
      </div>
    </div>
  </div>
);

export default NotFoundPage;
